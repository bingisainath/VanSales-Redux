import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container:{
        margin:10,
        backgroundColor:'lightblue',
        alignItems:'center',
        padding:10,
        borderColor:'blue',
        borderWidth:1,
        borderRadius:10,
        marginLeft:100,
        marginRight:100,
    }
}) 